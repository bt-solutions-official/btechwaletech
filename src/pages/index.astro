---
import BaseLayout from "@layouts/BaseLayout.astro";
import { createPageSeo } from "@lib/seo/seo-config";
import { createBreadcrumbs } from "@lib/breadcrumbs/breadcrumbs";
import { faqSchema } from "@lib/schema/schema";
import { siteContact } from "@data/site/site-config";
import { Image } from "astro:assets";
import heroImage from "../assets/images/heromain.png";
import cricketTakenImg from "../assets/images/crickettaken.png";

const faqs = [
  {
    question: "Do you build both static and dynamic websites?",
    answer:
      "Yes. We build static websites for SEO pages, portfolios, landing pages, and business sites, plus dynamic websites and web apps with dashboards, logins, databases, APIs, payments, and admin panels.",
  },
  {
    question: "Which IT services does Btechwale provide?",
    answer:
      "BtechWaleTech is a freelance IT services group providing website development, web design, deployment, SEO, ecommerce, custom software, AI automation, AI agents, multi-agent workflows, data analysis, and long-term support.",
  },
  {
    question: "Do you work with businesses outside India?",
    answer:
      "Yes. We are India-first and remote-friendly, so we can support clients in India first and expand delivery for the USA, UAE, UK, Australia, Singapore, and other global markets.",
  },
  {
    question: "Will my website be SEO friendly from day one?",
    answer:
      "Every website is planned with clean URLs, semantic HTML, title tags, meta descriptions, canonical URLs, sitemap support, schema foundations, performance basics, and crawlable static content where possible.",
  },
  {
    question: "Do you handle everything end to end, including hosting and deployment?",
    answer:
      "Yes. We handle the full delivery flow: requirements, page structure, web design, frontend and backend development, SEO setup, hosting, deployment, and testing. You are not coordinating between separate providers, and after hosting goes live you get 5 months of maintenance included free.",
  },
  {
    question: "What is included in the 5 months of free maintenance?",
    answer:
      "After your site is hosted and live, the next 5 months of maintenance cost nothing extra. That covers content and text updates, bug fixes, dependency and security updates, backups, uptime and speed checks, and basic SEO health checks. After that you can continue on a monthly plan from ₹2,000 or just contact us when something needs changing.",
  },
  {
    question: "How much does a website cost?",
    answer:
      "A static website starts from ₹5,000 and a 30-page SEO website handled from design through to deployment starts from ₹10,000. Ecommerce starts from ₹25,000 and custom web applications from ₹40,000. These are starting points: the final cost depends on pages, features, integrations, and content, so you get an itemised estimate.",
  },
  {
    question: "Do you provide AI automation and data analysis services?",
    answer:
      "Yes. We build practical AI automations, AI agents, workflow automation, dashboards, reporting systems, data cleaning, data analysis, and integrations that reduce manual work and improve decisions.",
  },
];

const page = createPageSeo({
  title: "Freelance IT Services, Web Development & AI Automation",
  description:
    "Freelance IT services in India for web development, web design, SEO, ecommerce, AI automation, AI agents, data analysis, deployment, and support.",
  canonicalPath: "/",
  h1: "Freelance IT services for websites, AI automation, and data analysis",
  intro:
    "BtechWaleTech is a small freelance services group helping businesses plan, design, build, deploy, optimize, and maintain practical digital systems without heavy agency overhead.",
  breadcrumbs: createBreadcrumbs([]),
  keywords: [
    "freelance IT services India",
    "freelance web development India",
    "website development services India",
    "website design and development India",
    "static website development",
    "dynamic website development",
    "custom web application development",
    "software development services India",
    "SEO services India",
    "local SEO services",
    "technical SEO agency",
    "ecommerce website development",
    "online store development India",
    "AI development services",
    "AI automation services India",
    "AI agents development",
    "multi agent AI workflows",
    "data analysis services India",
    "business automation services India",
    "website maintenance services",
    "website deployment services",
    "website redesign services",
    "hire web developers India",
    "web development Gurgaon",
    "web development Mumbai",
    "web development Bangalore",
    "website development Delhi NCR",
    "global IT services provider",
    "offshore web development India",
    "responsive website design",
    "freelance web developer India",
  ],
  ogImage: {
    url: "/banner_logo.png",
    alt: "Btechwale banner logo",
    width: 1536,
    height: 1024,
  },
});

const services = [
  {
    name: "Website development",
    summary:
      "Static and dynamic websites, landing pages, portfolios, business sites, and web apps built from design to deployment.",
    points: ["Business websites", "Landing pages", "Dynamic web apps", "Hosting and deployment"],
    href: "/services/web-development/",
    size: "lg",
  },
  {
    name: "SEO services",
    summary: "Technical SEO, local SEO, metadata, sitemap, schema, page structure, and content planning for search visibility.",
    points: ["Technical SEO setup", "Local SEO pages", "Schema and sitemap", "Search intent planning"],
    href: "/services/seo-services/",
    size: "md",
  },
  {
    name: "AI automation",
    summary: "Automations that reduce repetitive manual work and speed up business operations.",
    points: ["Workflow automation", "AI task handling", "Form and CRM flows", "Chat and email automation"],
    size: "sm",
  },
  {
    name: "AI agents",
    summary: "Custom assistant systems that use tools, prompts, and workflows for real tasks.",
    points: ["AI assistants", "Meta-agent workflows", "Tool-based agents", "Business process agents"],
    size: "sm",
  },
  {
    name: "Data analysis",
    summary: "Turn raw data into clear reports, dashboards, and decision-ready insights.",
    points: ["Data cleaning", "Excel and Sheets automation", "Dashboards", "Reports and insights"],
    size: "sm",
  },
  {
    name: "Software & web apps",
    summary: "Custom digital tools for internal teams, customers, and business workflows.",
    points: ["Admin panels", "Dashboards", "APIs and integrations", "Portals and tools"],
    size: "sm",
  },
  {
    name: "Ecommerce",
    summary: "Online store setup and optimization for selling products or services online.",
    points: ["Store setup", "Product pages", "Payments and checkout", "Performance tuning"],
    size: "sm",
  },
  {
    name: "Maintenance & support",
    summary: "Keep your website, app, SEO setup, and deployment running cleanly after launch.",
    points: ["Bug fixes", "Content updates", "Backup checks", "SEO and speed checks"],
    size: "sm",
  },
];

const marquee = [
  "Static websites",
  "Dynamic web apps",
  "Website design",
  "Deployment",
  "Custom software",
  "SEO & content",
  "Ecommerce",
  "AI automation",
  "AI agents",
  "Meta agents",
  "Data analysis",
  "Workflow automation",
  "Maintenance & support",
  "Performance & Core Web Vitals",
  "Schema & sitemaps",
  "Freelance IT services",
];

const stats = [
  { value: 3, suffix: "", label: "Engineers you work with directly" },
  { value: 5, suffix: "", label: "Months maintenance included free" },
  { value: 150, suffix: "+", label: "Indian cities with local pages" },
  { value: 100, suffix: "%", label: "Code and content ownership" },
];

const builds = [
  {
    name: "Static websites",
    tag: "Speed-first",
    summary: "Business websites, landing pages, portfolios, and SEO pages rendered as fast, crawlable HTML.",
    points: ["Fast loading", "Clean SEO structure", "Simple hosting", "Secure static output"],
  },
  {
    name: "Dynamic websites & web apps",
    tag: "Feature-first",
    summary: "Dashboards, databases, forms, bookings, payments, and custom tools built around real workflows.",
    points: ["Custom APIs", "User accounts", "Admin panels", "Deployment support"],
  },
];

const process = [
  ["Understand", "Clarify your goal, users, service intent, pages, features, data, and expected business outcome."],
  ["Build", "Create the website, web app, automation, AI agent, or data workflow with clean structure and SEO basics."],
  ["Deploy", "Set up hosting, production build, tracking basics, sitemap, performance checks, and post-launch support."],
];

const comparison = [
  {
    aspect: "Who you speak to",
    agency: "Account managers & coordinators",
    cheap: "Unknown freelancer, no guarantee",
    us: "Recently graduated engineers directly",
  },
  {
    aspect: "Project clarity",
    agency: "Briefs passed down, detail lost",
    cheap: "Vague scope, frequent surprises",
    us: "Itemised scope before work starts",
  },
  {
    aspect: "Post-launch support",
    agency: "Paid retainer required",
    cheap: "Usually disappears",
    us: "5 months free maintenance",
  },
  {
    aspect: "Code ownership",
    agency: "Often locked to their systems",
    cheap: "Varies, often no handover",
    us: "100% yours from day one",
  },
  {
    aspect: "SEO from the start",
    agency: "Separate SEO team, extra cost",
    cheap: "Rarely considered",
    us: "Built into every project",
  },
];

const locations = [
  ["Gurgaon", "/india/haryana/gurgaon/"],
  ["Faridabad", "/india/haryana/faridabad/"],
  ["Delhi", "/it-services/delhi/new-delhi/"],
  ["Noida", "/india/uttar-pradesh/noida/"],
  ["Mumbai", "/india/maharashtra/mumbai/"],
  ["Pune", "/india/maharashtra/pune/"],
  ["Bangalore", "/india/karnataka/bangalore/"],
  ["Hyderabad", "/india/telangana/hyderabad/"],
  ["Chennai", "/india/tamil-nadu/chennai/"],
  ["Kolkata", "/india/west-bengal/kolkata/"],
  ["Ahmedabad", "/india/gujarat/ahmedabad/"],
  ["Jaipur", "/india/rajasthan/jaipur/"],
  ["Lucknow", "/india/uttar-pradesh/lucknow/"],
  ["Kochi", "/india/kerala/kochi/"],
  ["Indore", "/it-services/madhya-pradesh/indore/"],
  ["Patna", "/it-services/bihar/patna/"],
  ["Bhubaneswar", "/it-services/odisha/bhubaneswar/"],
  ["Ludhiana", "/it-services/punjab/ludhiana/"],
  ["Guwahati", "/it-services/assam/guwahati/"],
  ["Dehradun", "/it-services/uttarakhand/dehradun/"],
];

const stateHubs = [
  ["Delhi", "/india/delhi/"],
  ["Haryana", "/india/haryana/"],
  ["Maharashtra", "/india/maharashtra/"],
  ["Karnataka", "/india/karnataka/"],
  ["Uttar Pradesh", "/india/uttar-pradesh/"],
  ["Telangana", "/india/telangana/"],
  ["Tamil Nadu", "/india/tamil-nadu/"],
  ["Rajasthan", "/india/rajasthan/"],
  ["Gujarat", "/india/gujarat/"],
  ["West Bengal", "/india/west-bengal/"],
  ["Kerala", "/india/kerala/"],
  ["All 30 states", "/it-services/"],
];
---

<BaseLayout meta={page} schema={[faqSchema(faqs)]}>

  <!-- ===================== HERO ===================== -->
  <section class="hero" aria-labelledby="home-heading">
    <div class="hero-inner">
      <div class="hero-copy">
        <p class="eyebrow">India-first freelance IT services group</p>
        <h1 id="home-heading">
          Freelance IT services for websites, AI automation, and data analysis
        </h1>
        <p class="lede">{page.intro}</p>
        <div class="hero-actions">
          <a class="btn-primary" href={siteContact.whatsappHref} target="_blank" rel="noopener noreferrer">
            <span>WhatsApp Us</span>
            <span class="btn-arrow" aria-hidden="true">&rarr;</span>
          </a>
          <a class="btn-ghost" href="#proof-of-work">See our work</a>
        </div>

        <!-- Service pills -->
        <ul class="hero-pills" aria-label="What we do">
          <li>Websites</li>
          <li>AI Automation</li>
          <li>Data Systems</li>
          <li>Internal Tools</li>
        </ul>

        <!-- Based in India tagline -->
        <p class="hero-origin">
          <span class="origin-dot" aria-hidden="true"></span>
          Based in India &middot; Working remotely worldwide
        </p>
      </div>

      <!-- Hero Image -->
      <div class="hero-image-panel">
        <div class="hero-img-wrap">
          <Image
            src={heroImage}
            alt="BtechWaleTech workspace — laptop showing digital solutions, branded coffee mug, notebooks and office setup"
            loading="eager"
            class="hero-img"
            widths={[640, 960, 1440]}
            sizes="(max-width: 768px) 100vw, 55vw"
          />
        </div>
      </div>
    </div>
  </section>

  <!-- ===================== MARQUEE ===================== -->
  <div class="marquee" aria-hidden="true">
    <div class="marquee-track">
      {[0, 1].map(() => (
        <div class="marquee-set">
          {marquee.map((item) => (
            <span class="marquee-item">{item}</span>
          ))}
        </div>
      ))}
    </div>
  </div>

  <!-- ===================== STATS ===================== -->
  <section class="stats-section" aria-label="BtechWaleTech in numbers">
    <div class="section-wrap">
      <ul class="stat-band">
        {stats.map((s) => (
          <li>
            <span class="stat-num">{s.value}{s.suffix}</span>
            <span class="stat-label">{s.label}</span>
          </li>
        ))}
      </ul>
    </div>
  </section>

  <!-- ===================== SERVICES ===================== -->
  <section class="services" aria-labelledby="services-heading">
    <div class="section-wrap">
      <div class="section-head">
        <p class="eyebrow">Core services</p>
        <h2 id="services-heading">Practical IT services for businesses that need work done properly</h2>
        <p class="section-note">
          We help with the full digital work stack: website planning, design, development, SEO setup, AI automation,
          AI agents, data analysis, ecommerce, deployment, and maintenance.
        </p>
      </div>
      <div class="bento">
        {services.map((s) => {
          const inner = (
            <div class="card-core">
              <h3>{s.name}</h3>
              <p>{s.summary}</p>
              <ul class="service-points">
                {s.points.map((point) => (
                  <li>{point}</li>
                ))}
              </ul>
              {s.href && <span class="card-arrow" aria-hidden="true">&rarr;</span>}
            </div>
          );
          return s.href ? (
            <a class={`card card-${s.size}`} href={s.href}>
              {inner}
            </a>
          ) : (
            <div class={`card card-${s.size}`}>
              {inner}
            </div>
          );
        })}
      </div>
    </div>
  </section>

  <!-- ===================== OUR WORKS / PROOF OF WORK ===================== -->
  <section class="proof-of-work" id="proof-of-work" aria-labelledby="works-heading">
    <div class="section-wrap">
      <div class="section-head">
        <p class="eyebrow">Our works / Proof of work</p>
        <h2 id="works-heading">Featured live project: CricketTaken</h2>
        <p class="section-note">
          A high-performance cricket records, dynamic scorecards & tournament analytics platform engineered for speed, deep data archiving, and organic sports search capture.
        </p>
      </div>

      <div class="work-showcase">
        <!-- Left: Project Information & Interactive Animated Tabs -->
        <div class="work-details">
          <div class="work-status-row">
            <span class="live-pulse-badge">
              <span class="pulse-dot"></span>
              Live in production
            </span>
            <span class="work-category">Web Platform &middot; Sports Intelligence</span>
          </div>

          <h3 class="work-title">CricketTaken &mdash; All Cricket Records &amp; Live Match History</h3>
          <p class="work-desc">
            Engineered from the ground up as a lightning-fast static-first sports database. Powers deep tournament records (IPL, ICC World Cup, SA20, BBL, The Ashes), head-to-head match histories, player career breakdowns, and interactive real-time fan polls.
          </p>

          <!-- Interactive Tabs -->
          <div class="work-tabs" role="tablist" aria-label="Project details">
            <button class="work-tab active" data-work-tab="overview" role="tab" aria-selected="true" type="button">
              Overview
            </button>
            <button class="work-tab" data-work-tab="features" role="tab" aria-selected="false" type="button">
              Key Features
            </button>
            <button class="work-tab" data-work-tab="stack" role="tab" aria-selected="false" type="button">
              Technical Stack
            </button>
          </div>

          <!-- Tab Panels -->
          <div class="work-panels">
            <div class="work-panel active" data-work-panel="overview" role="tabpanel">
              <ul class="work-checklist">
                <li>
                  <strong>Comprehensive Record Archives:</strong> Instant access to historical scorecards, tournament records, and player statistics across 15+ international leagues.
                </li>
                <li>
                  <strong>Sub-second Navigation:</strong> Pre-rendered static pages combined with reactive islands for zero layout shift and instant page transitions.
                </li>
                <li>
                  <strong>High-Intent Search Architecture:</strong> 1,000+ programmatic SEO routes driving organic search capture for player queries and match archives.
                </li>
              </ul>
            </div>

            <div class="work-panel" data-work-panel="features" role="tabpanel">
              <ul class="work-checklist">
                <li>
                  <strong>Interactive Fan Polls:</strong> Real-time voting widgets with instant client-side updates and anti-spam verification.
                </li>
                <li>
                  <strong>Playing XI Builder:</strong> Dynamic team constructor with balance analysis and custom match simulations.
                </li>
                <li>
                  <strong>Live Dark/Light Editorial Theme:</strong> High-contrast sports UI tailored for live match viewing in any environment.
                </li>
              </ul>
            </div>

            <div class="work-panel" data-work-panel="stack" role="tabpanel">
              <ul class="work-checklist">
                <li>
                  <strong>Astro &amp; TypeScript:</strong> Island architecture delivering 100/100 Core Web Vitals score.
                </li>
                <li>
                  <strong>TailwindCSS Design System:</strong> Custom dark-theme sports palette with responsive container queries.
                </li>
                <li>
                  <strong>Optimized Production Hosting:</strong> Edge deployment with automated cache invalidation and Schema.org rich results.
                </li>
              </ul>
            </div>
          </div>

          <!-- Actions -->
          <div class="work-actions">
            <a class="btn-primary" href="https://crickettaken.in/" target="_blank" rel="noopener noreferrer">
              <span>Visit crickettaken.in</span>
              <span class="btn-arrow" aria-hidden="true">&rarr;</span>
            </a>
            <a class="btn-ghost" href={siteContact.whatsappHref} target="_blank" rel="noopener noreferrer">
              <span>Discuss a similar project</span>
            </a>
          </div>
        </div>

        <!-- Right: Browser Mockup Showcase Cover -->
        <div class="work-mockup-wrapper">
          <div class="browser-frame">
            <!-- Browser Header -->
            <div class="browser-bar">
              <div class="browser-dots" aria-hidden="true">
                <span></span>
                <span></span>
                <span></span>
              </div>
              <div class="browser-url">
                <span class="lock-icon material-symbols-outlined" aria-hidden="true">lock</span>
                https://crickettaken.in
              </div>
              <a class="browser-external-link" href="https://crickettaken.in/" target="_blank" rel="noopener noreferrer" title="Open crickettaken.in in new tab">
                <span class="material-symbols-outlined" aria-hidden="true">open_in_new</span>
              </a>
            </div>

            <!-- Browser Content Image -->
            <a class="browser-viewport" href="https://crickettaken.in/" target="_blank" rel="noopener noreferrer">
              <Image
                src={cricketTakenImg}
                alt="CricketTaken — Cricket records, scorecards and match history platform"
                loading="lazy"
                class="work-cover-img"
                widths={[640, 960, 1280]}
                sizes="(max-width: 768px) 100vw, 50vw"
              />
              <div class="browser-overlay">
                <span class="overlay-cta">
                  <span>Open Live Project</span>
                  <span class="btn-arrow">&rarr;</span>
                </span>
              </div>
            </a>
          </div>

          <!-- Quick Metrics Bar -->
          <div class="work-metrics">
            <div class="metric-item">
              <span class="metric-val">15+</span>
              <span class="metric-lbl">Leagues Tracked</span>
            </div>
            <div class="metric-item">
              <span class="metric-val">&lt;400ms</span>
              <span class="metric-lbl">Page Speed</span>
            </div>
            <div class="metric-item">
              <span class="metric-val">100%</span>
              <span class="metric-lbl">Responsive</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ===================== HERITAGE ADVANTAGE ===================== -->
  <section class="advantage" aria-labelledby="advantage-heading">
    <div class="section-wrap">
      <div class="section-head">
        <p class="eyebrow">The heritage advantage</p>
        <h2 id="advantage-heading">Engineering ethics over speed, shortcuts, and hand-offs</h2>
        <p class="section-note">
          We are not a large agency with layers of management, nor a cheap freelancer who disappears post-launch.
          We are a focused group of recently graduated engineers who own your project from start to finish.
        </p>
      </div>
      <div class="comparison-table-wrap">
        <table class="comparison-table" role="table">
          <thead>
            <tr>
              <th scope="col" class="col-aspect">What matters</th>
              <th scope="col" class="col-agency">Traditional agency</th>
              <th scope="col" class="col-cheap">Cheap freelancer</th>
              <th scope="col" class="col-us">BtechWaleTech</th>
            </tr>
          </thead>
          <tbody>
            {comparison.map((row) => (
              <tr>
                <td class="td-aspect">{row.aspect}</td>
                <td class="td-agency">{row.agency}</td>
                <td class="td-cheap">{row.cheap}</td>
                <td class="td-us">
                  <span class="td-us-inner">
                    <span class="td-check material-symbols-outlined" aria-hidden="true">check_circle</span>
                    {row.us}
                  </span>
                </td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>
    </div>
  </section>

  <!-- ===================== BUILD TYPES ===================== -->
  <section class="builds" aria-labelledby="builds-heading">
    <div class="section-wrap">
      <div class="section-head">
        <p class="eyebrow">Static &amp; dynamic</p>
        <h2 id="builds-heading">Every kind of website, built the right way</h2>
        <p class="section-note">
          Some businesses need a fast static website. Others need a dynamic web app, dashboard, automation, or AI-powered
          workflow. We help choose the right setup before development starts.
        </p>
      </div>
      <div class="build-grid">
        {builds.map((b) => (
          <a class="build-card" href="/services/web-development/">
            <div class="card-core">
              <p class="build-tag">{b.tag}</p>
              <h3>{b.name}</h3>
              <p class="build-summary">{b.summary}</p>
              <ul class="build-points">
                {b.points.map((p) => (
                  <li>{p}</li>
                ))}
              </ul>
              <span class="card-arrow" aria-hidden="true">&rarr;</span>
            </div>
          </a>
        ))}
      </div>
    </div>
  </section>

  <!-- ===================== PROCESS ===================== -->
  <section class="process" aria-labelledby="process-heading">
    <div class="section-wrap">
      <div class="section-head">
        <p class="eyebrow">How work stays clean</p>
        <h2 id="process-heading">Clear requirement, clean build, smooth deployment</h2>
      </div>
      <ol class="steps">
        {process.map(([step, detail], i) => (
          <li>
            <span class="step-num" aria-hidden="true">{String(i + 1).padStart(2, "0")}</span>
            <h3>{step}</h3>
            <p>{detail}</p>
          </li>
        ))}
      </ol>
    </div>
  </section>

  <!-- ===================== LOCATIONS ===================== -->
  <section class="locations" aria-labelledby="locations-heading">
    <div class="section-wrap">
      <div class="section-head">
        <p class="eyebrow">Pan-India coverage</p>
        <h2 id="locations-heading">Covering 150+ cities across all 30 states in India</h2>
        <p class="section-note">
          We provide full-stack IT services, web development, SEO, and AI automation to clients across major metro hubs, tier-2 cities, and businesses throughout India.
        </p>
      </div>
      <div class="city-cloud">
        {locations.map(([name, href]) => (
          <a class="city-chip" href={href}>{name}</a>
        ))}
      </div>
      <p class="cloud-label">Browse by state</p>
      <div class="city-cloud city-cloud-muted">
        {stateHubs.map(([name, href]) => (
          <a class="city-chip" href={href}>{name}</a>
        ))}
      </div>
    </div>
  </section>

  <!-- ===================== FAQ ===================== -->
  <section class="faq" aria-labelledby="faq-heading">
    <div class="section-wrap">
      <div class="section-head">
        <p class="eyebrow">Questions</p>
        <h2 id="faq-heading">Answers before you even ask</h2>
      </div>
      <div class="faq-list">
        {faqs.map((f) => (
          <details class="faq-item">
            <summary>
              <span>{f.question}</span>
              <span class="faq-mark" aria-hidden="true">+</span>
            </summary>
            <p>{f.answer}</p>
          </details>
        ))}
      </div>
    </div>
  </section>

  <!-- ===================== CLOSING CTA ===================== -->
  <section class="closing" aria-labelledby="cta-heading">
    <div class="section-wrap">
      <div class="closing-inner">
        <p class="eyebrow">Next step</p>
        <h2 id="cta-heading">Need a website, automation, AI agent, or data workflow?</h2>
        <p class="closing-note">
          Tell us what you want to build or fix. We will reply with a clear scope, honest estimate, and practical delivery
          plan for your website, SEO setup, AI automation, data analysis, or custom IT work.
        </p>
        <div class="closing-actions">
          <a class="btn-primary" href={siteContact.whatsappHref} target="_blank" rel="noopener noreferrer">
            <span>WhatsApp Us</span>
            <span class="btn-arrow" aria-hidden="true">&rarr;</span>
          </a>
        </div>
      </div>
    </div>
  </section>

</BaseLayout>

<script>
  // Interactive Tab Switcher for Proof of Work Section
  const initWorkTabs = () => {
    const tabs = document.querySelectorAll<HTMLButtonElement>('[data-work-tab]');
    const panels = document.querySelectorAll<HTMLElement>('[data-work-panel]');

    tabs.forEach((tab) => {
      tab.addEventListener('click', () => {
        const target = tab.getAttribute('data-work-tab');
        tabs.forEach((t) => {
          const isActive = t === tab;
          t.classList.toggle('active', isActive);
          t.setAttribute('aria-selected', isActive ? 'true' : 'false');
        });
        panels.forEach((panel) => {
          const isMatch = panel.getAttribute('data-work-panel') === target;
          panel.classList.toggle('active', isMatch);
        });
      });
    });
  };

  initWorkTabs();
</script>



<style>
  /* ================== TOKENS ================== */
  :root {
    --paper: #FDFCFB;
    --surface: #F8F9FA;
    --surface-low: #F3F4F5;
    --midnight: #0F172A;
    --slate: #334155;
    --slate-muted: #64748B;
    --gold: #D4AF37;
    --gold-muted: #C5A059;
    --gold-light: #FAF5E8;
    --hairline: rgba(15, 23, 42, 0.10);
    --ease: cubic-bezier(0.16, 1, 0.3, 1);
  }

  :global(body) {
    background: var(--paper);
    color: var(--midnight);
  }

  :global(::selection) {
    background: var(--gold);
    color: var(--midnight);
  }

  h1, h2, h3 {
    margin: 0;
    font-family: 'Hanken Grotesk', system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
    color: var(--midnight);
    letter-spacing: -0.02em;
    font-weight: 700;
  }

  p { margin: 0; }

  /* ================== LAYOUT WRAPPER ================== */
  .section-wrap {
    max-width: 1280px;
    margin: 0 auto;
    padding: 0 clamp(1.25rem, 5vw, 5rem);
  }

  /* ================== EYEBROW ================== */
  .eyebrow {
    display: inline-flex;
    align-items: center;
    gap: 0.65rem;
    margin: 0 0 1.25rem;
    color: var(--gold-muted);
    font-family: 'Hanken Grotesk', system-ui, sans-serif;
    font-size: 0.75rem;
    font-weight: 600;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  .eyebrow::before {
    display: inline-block;
    width: 1.75rem;
    height: 1px;
    background: var(--gold-muted);
    content: "";
    flex-shrink: 0;
  }

  .eyebrow-light {
    color: rgba(212, 175, 55, 0.7);
  }
  .eyebrow-light::before {
    background: rgba(212, 175, 55, 0.7);
  }



  /* ================== SECTION HEAD ================== */
  .section-head {
    max-width: 48rem;
    margin-bottom: clamp(2.5rem, 5vw, 4rem);
  }

  .section-head h2 {
    font-size: clamp(1.85rem, 3.5vw, 2.75rem);
    line-height: 1.15;
    font-weight: 600;
    margin-bottom: 1rem;
  }

  .section-note {
    color: var(--slate);
    font-size: 1.05rem;
    line-height: 1.7;
    margin-top: 0.75rem;
  }

  /* ================== BUTTONS ================== */
  .hero-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
    margin-top: 2rem;
  }

  .btn-primary, .btn-ghost, .btn-cream {
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    padding: 0.9375rem 1.75rem;
    font-family: 'Hanken Grotesk', system-ui, sans-serif;
    font-size: 0.8125rem;
    font-weight: 600;
    letter-spacing: 0.07em;
    text-transform: uppercase;
    text-decoration: none;
    border-radius: 2px;
    border: 1px solid;
    transition: all 250ms var(--ease);
    white-space: nowrap;
  }

  .btn-primary {
    background: var(--midnight);
    color: var(--paper);
    border-color: var(--midnight);
  }
  .btn-primary:hover { background: var(--slate); border-color: var(--slate); transform: translateY(-1px); box-shadow: 0 6px 20px rgba(15,23,42,0.15); }

  .btn-ghost {
    background: transparent;
    color: var(--midnight);
    border-color: var(--midnight);
  }
  .btn-ghost:hover { background: var(--surface-low); transform: translateY(-1px); }

  .btn-cream {
    background: var(--paper);
    color: var(--midnight);
    border-color: var(--paper);
    box-shadow: 0 4px 20px rgba(0,0,0,0.2);
  }
  .btn-cream:hover { background: #fff; transform: translateY(-1px); box-shadow: 0 8px 28px rgba(0,0,0,0.25); }

  .btn-arrow {
    display: inline-block;
    transition: transform 300ms var(--ease);
  }
  .btn-primary:hover .btn-arrow,
  .btn-cream:hover .btn-arrow {
    transform: translateX(4px);
  }

  /* ================== HERO ================== */
  .hero {
    padding: clamp(2rem, 4vw, 3.5rem) 0 clamp(2.5rem, 5vw, 4rem);
    border-bottom: 1px solid var(--hairline);
    overflow: hidden;
  }

  .hero-inner {
    max-width: 1280px;
    margin: 0 auto;
    padding: 0 clamp(1.25rem, 5vw, 5rem);
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(0, 1.15fr);
    gap: clamp(2rem, 4vw, 3.5rem);
    align-items: center;
  }

  .hero-copy {
    display: flex;
    flex-direction: column;
    justify-content: center;
  }

  h1 {
    font-size: clamp(2.2rem, 4vw, 3.25rem);
    line-height: 1.12;
    font-weight: 700;
    margin-bottom: 1.25rem;
  }

  .lede {
    color: var(--slate);
    font-size: clamp(0.95rem, 1.3vw, 1.05rem);
    line-height: 1.7;
    max-width: 32rem;
  }

  /* Service pills row */
  .hero-pills {
    display: flex;
    flex-wrap: wrap;
    gap: 0.65rem 1.25rem;
    margin: 1.75rem 0 0;
    padding: 1.5rem 0 0;
    border-top: 1px solid var(--hairline);
    list-style: none;
  }

  .hero-pills li {
    display: inline-flex;
    align-items: center;
    gap: 0.35rem;
    color: var(--slate);
    font-family: 'Hanken Grotesk', system-ui, sans-serif;
    font-size: 0.82rem;
    font-weight: 500;
    white-space: nowrap;
  }

  .pill-icon {
    font-size: 1.1rem;
    color: var(--gold);
    font-variation-settings: 'FILL' 0, 'wght' 400;
  }

  /* Based in India tagline */
  .hero-origin {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    margin-top: 1.25rem;
    color: var(--slate-muted);
    font-family: 'Hanken Grotesk', system-ui, sans-serif;
    font-size: 0.78rem;
    font-weight: 500;
    letter-spacing: 0.02em;
  }

  .origin-dot {
    display: inline-block;
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--gold);
    flex-shrink: 0;
  }

  /* ================== HERO IMAGE PANEL ================== */
  .hero-image-panel {
    position: relative;
    width: 100%;
  }

  .hero-img-wrap {
    position: relative;
    width: 100%;
    border-radius: 6px;
    overflow: hidden;
    border: 1px solid var(--hairline);
    box-shadow: 0 20px 45px -15px rgba(15, 23, 42, 0.12);
    background: var(--surface-low);
  }

  .hero-img {
    display: block;
    width: 100%;
    height: auto;
    object-fit: cover;
    transition: transform 600ms var(--ease);
  }

  .hero-image-panel:hover .hero-img {
    transform: scale(1.012);
  }



  /* ================== MARQUEE ================== */
  .marquee {
    overflow: hidden;
    padding: 1.1rem 0;
    border-top: 1px solid var(--hairline);
    border-bottom: 1px solid var(--hairline);
    background: var(--surface-low);
    mask-image: linear-gradient(90deg, transparent, #000 8%, #000 92%, transparent);
  }

  .marquee-track {
    display: flex;
    width: max-content;
    animation: slide 40s linear infinite;
  }

  .marquee:hover .marquee-track { animation-play-state: paused; }

  .marquee-set { display: flex; align-items: center; }

  .marquee-item {
    color: var(--slate);
    font-family: 'Hanken Grotesk', system-ui, sans-serif;
    font-size: 0.85rem;
    font-weight: 500;
    letter-spacing: 0.05em;
    text-transform: uppercase;
    white-space: nowrap;
  }

  .marquee-item::after {
    margin: 0 1.75rem;
    color: var(--gold);
    content: "◆";
    font-size: 0.45rem;
    vertical-align: middle;
  }

  @keyframes slide { to { transform: translateX(-50%); } }

  /* ================== STATS ================== */
  .stats-section {
    padding: clamp(4rem, 7vw, 6rem) 0;
    background: var(--midnight);
    border-top: 1px solid rgba(255,255,255,0.04);
  }

  .stat-band {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 1px;
    margin: 0;
    padding: 0;
    list-style: none;
    background: rgba(255,255,255,0.06);
    border: 1px solid rgba(255,255,255,0.06);
    border-radius: 2px;
    overflow: hidden;
  }

  .stat-band li {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    align-items: center;
    text-align: center;
    padding: clamp(2rem, 4vw, 3rem) clamp(1rem, 3vw, 2rem);
    background: var(--midnight);
    position: relative;
  }

  .stat-band li::after {
    content: "";
    position: absolute;
    bottom: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 2.5rem;
    height: 2px;
    background: var(--gold);
    opacity: 0;
    transition: opacity 300ms ease;
  }

  .stat-band li:hover::after { opacity: 1; }

  .stat-num {
    color: #F8F9FA;
    font-family: 'Hanken Grotesk', system-ui, -apple-system, sans-serif;
    font-size: clamp(2.5rem, 5vw, 3.5rem);
    font-weight: 700;
    letter-spacing: -0.02em;
    line-height: 1;
  }

  .stat-label {
    color: var(--slate-muted);
    font-family: 'Hanken Grotesk', system-ui, sans-serif;
    font-size: 0.85rem;
    font-weight: 400;
    line-height: 1.4;
    max-width: 12rem;
  }

  /* ================== SERVICES ================== */
  .services {
    padding: clamp(3.5rem, 6vw, 5.5rem) 0;
    background: var(--surface);
    border-top: 1px solid var(--hairline);
  }

  .bento {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 1px;
    background: var(--hairline);
    border: 1px solid var(--hairline);
    border-radius: 2px;
    overflow: hidden;
  }

  .card {
    display: block;
    background: var(--paper);
    text-decoration: none;
    transition: background-color 250ms ease;
  }

  .card:hover { background: #fff; }

  .card-core {
    position: relative;
    padding: clamp(1.25rem, 2vw, 1.6rem);
    height: 100%;
    display: flex;
    flex-direction: column;
  }

  .card-core h3 {
    font-size: clamp(1.05rem, 1.4vw, 1.2rem);
    font-weight: 600;
    margin-bottom: 0.5rem;
    line-height: 1.25;
    padding-right: 1.75rem;
  }

  .card-core > p {
    color: var(--slate);
    font-size: 0.84rem;
    line-height: 1.55;
    margin-bottom: 1rem;
    flex: 1;
  }

  .service-points {
    display: grid;
    grid-template-columns: 1fr;
    gap: 0.35rem;
    margin: 0;
    padding: 0;
    list-style: none;
    border-top: 1px solid var(--hairline);
    padding-top: 0.85rem;
  }

  .service-points li {
    position: relative;
    padding-left: 0.85rem;
    color: var(--slate-muted);
    font-family: 'Hanken Grotesk', system-ui, sans-serif;
    font-size: 0.78rem;
    line-height: 1.35;
  }

  .service-points li::before {
    position: absolute;
    left: 0;
    top: 0.5em;
    width: 0.28rem;
    height: 0.28rem;
    border-radius: 50%;
    background: var(--gold);
    content: "";
  }

  .card-arrow {
    position: absolute;
    top: clamp(1.25rem, 2vw, 1.6rem);
    right: clamp(1.25rem, 2vw, 1.6rem);
    display: inline-grid;
    place-items: center;
    width: 1.75rem;
    height: 1.75rem;
    border: 1px solid var(--hairline);
    border-radius: 1px;
    color: var(--slate-muted);
    font-size: 0.85rem;
    transition: all 250ms var(--ease);
  }

  a.card:hover .card-arrow {
    background: var(--midnight);
    border-color: var(--midnight);
    color: var(--paper);
    transform: translate(2px, -2px);
  }

  /* ================== PROOF OF WORK ================== */
  .proof-of-work {
    padding: clamp(3.5rem, 6vw, 5.5rem) 0;
    background: var(--paper);
    border-top: 1px solid var(--hairline);
  }

  .work-showcase {
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(0, 1.15fr);
    gap: clamp(2.5rem, 5vw, 4.5rem);
    align-items: center;
  }

  .work-details {
    display: flex;
    flex-direction: column;
  }

  .work-status-row {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 0.75rem;
    margin-bottom: 1rem;
  }

  .live-pulse-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.45rem;
    padding: 0.25rem 0.75rem;
    background: rgba(34, 197, 94, 0.1);
    border: 1px solid rgba(34, 197, 94, 0.25);
    border-radius: 999px;
    color: #166534;
    font-family: 'Hanken Grotesk', system-ui, sans-serif;
    font-size: 0.75rem;
    font-weight: 600;
    letter-spacing: 0.03em;
  }

  .pulse-dot {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: #22c55e;
    box-shadow: 0 0 0 0 rgba(34, 197, 94, 0.7);
    animation: pulse-ring 2s infinite;
  }

  @keyframes pulse-ring {
    0% { transform: scale(0.95); box-shadow: 0 0 0 0 rgba(34, 197, 94, 0.7); }
    70% { transform: scale(1); box-shadow: 0 0 0 6px rgba(34, 197, 94, 0); }
    100% { transform: scale(0.95); box-shadow: 0 0 0 0 rgba(34, 197, 94, 0); }
  }

  .work-category {
    color: var(--slate-muted);
    font-family: 'Hanken Grotesk', system-ui, sans-serif;
    font-size: 0.8rem;
    font-weight: 500;
  }

  .work-title {
    font-size: clamp(1.4rem, 2.2vw, 1.85rem);
    font-weight: 700;
    margin-bottom: 0.75rem;
    line-height: 1.25;
  }

  .work-desc {
    color: var(--slate);
    font-size: 0.92rem;
    line-height: 1.65;
    margin-bottom: 1.5rem;
  }

  /* Tabs */
  .work-tabs {
    display: flex;
    gap: 0.5rem;
    border-bottom: 1px solid var(--hairline);
    margin-bottom: 1.25rem;
    padding-bottom: 0.25rem;
  }

  .work-tab {
    padding: 0.5rem 1rem;
    background: transparent;
    border: none;
    border-bottom: 2px solid transparent;
    color: var(--slate-muted);
    font-family: 'Hanken Grotesk', system-ui, sans-serif;
    font-size: 0.84rem;
    font-weight: 600;
    cursor: pointer;
    transition: all 200ms ease;
    margin-bottom: -1px;
  }

  .work-tab:hover {
    color: var(--midnight);
  }

  .work-tab.active {
    color: var(--midnight);
    border-bottom-color: var(--gold);
  }

  /* Panels */
  .work-panels {
    min-height: 10.5rem;
    margin-bottom: 1.75rem;
  }

  .work-panel {
    display: none;
    animation: fadeInTab 250ms ease;
  }

  .work-panel.active {
    display: block;
  }

  @keyframes fadeInTab {
    from { opacity: 0; transform: translateY(4px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .work-checklist {
    list-style: none;
    padding: 0;
    margin: 0;
    display: grid;
    gap: 0.75rem;
  }

  .work-checklist li {
    position: relative;
    padding-left: 1.35rem;
    color: var(--slate);
    font-family: 'Hanken Grotesk', system-ui, sans-serif;
    font-size: 0.875rem;
    line-height: 1.55;
  }

  .work-checklist li::before {
    position: absolute;
    left: 0;
    color: var(--gold);
    content: "✓";
    font-weight: 700;
  }

  .work-checklist strong {
    color: var(--midnight);
  }

  .work-actions {
    display: flex;
    flex-wrap: nowrap;
    align-items: center;
    gap: 0.75rem;
  }

  .work-actions .btn-primary,
  .work-actions .btn-ghost {
    padding: 0.85rem 1.25rem;
    font-size: 0.78rem;
    letter-spacing: 0.05em;
    white-space: nowrap;
    flex: 0 0 auto;
  }

  /* Mockup Frame */
  .work-mockup-wrapper {
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
  }

  .browser-frame {
    border-radius: 8px;
    overflow: hidden;
    background: #111827;
    border: 1px solid rgba(15, 23, 42, 0.15);
    box-shadow: 0 25px 60px -20px rgba(15, 23, 42, 0.25);
    transition: transform 400ms var(--ease), box-shadow 400ms var(--ease);
  }

  .browser-frame:hover {
    transform: translateY(-3px);
    box-shadow: 0 30px 70px -20px rgba(15, 23, 42, 0.35);
  }

  .browser-bar {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding: 0.65rem 1rem;
    background: #1e293b;
    border-bottom: 1px solid rgba(255, 255, 255, 0.08);
  }

  .browser-dots {
    display: flex;
    gap: 5px;
  }

  .browser-dots span {
    width: 9px;
    height: 9px;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.2);
  }

  .browser-dots span:nth-child(1) { background: #ef4444; }
  .browser-dots span:nth-child(2) { background: #f59e0b; }
  .browser-dots span:nth-child(3) { background: #10b981; }

  .browser-url {
    flex: 1;
    display: flex;
    align-items: center;
    gap: 0.35rem;
    padding: 0.25rem 0.75rem;
    background: rgba(0, 0, 0, 0.25);
    border-radius: 4px;
    color: #94a3b8;
    font-family: 'Hanken Grotesk', system-ui, monospace;
    font-size: 0.75rem;
    letter-spacing: 0.02em;
  }

  .lock-icon {
    font-size: 0.85rem;
    color: #10b981;
  }

  .browser-external-link {
    color: #94a3b8;
    display: inline-flex;
    align-items: center;
    transition: color 200ms ease;
  }

  .browser-external-link:hover {
    color: #f8fafc;
  }

  .browser-viewport {
    position: relative;
    display: block;
    overflow: hidden;
    text-decoration: none;
  }

  .work-cover-img {
    display: block;
    width: 100%;
    height: auto;
    object-fit: cover;
    transition: transform 600ms var(--ease);
  }

  .browser-viewport:hover .work-cover-img {
    transform: scale(1.02);
  }

  .browser-overlay {
    position: absolute;
    inset: 0;
    background: rgba(15, 23, 42, 0.6);
    display: flex;
    align-items: center;
    justify-content: center;
    opacity: 0;
    transition: opacity 300ms ease;
  }

  .browser-viewport:hover .browser-overlay {
    opacity: 1;
  }

  .overlay-cta {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.75rem 1.4rem;
    background: #f8f9fa;
    color: #0f172a;
    font-family: 'Hanken Grotesk', system-ui, sans-serif;
    font-size: 0.82rem;
    font-weight: 700;
    border-radius: 2px;
    letter-spacing: 0.05em;
    text-transform: uppercase;
    box-shadow: 0 10px 25px rgba(0, 0, 0, 0.3);
  }

  /* Metrics Bar */
  .work-metrics {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1px;
    background: var(--hairline);
    border: 1px solid var(--hairline);
    border-radius: 4px;
    overflow: hidden;
  }

  .metric-item {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    padding: 0.75rem 0.5rem;
    background: var(--surface-low);
  }

  .metric-val {
    font-family: 'Hanken Grotesk', system-ui, sans-serif;
    font-size: 1.15rem;
    font-weight: 700;
    color: var(--midnight);
    line-height: 1.2;
  }

  .metric-lbl {
    font-family: 'Hanken Grotesk', system-ui, sans-serif;
    font-size: 0.72rem;
    color: var(--slate-muted);
  }

  /* ================== ADVANTAGE ================== */
  .advantage {
    padding: clamp(3.5rem, 6vw, 5.5rem) 0;
    border-top: 1px solid var(--hairline);
  }

  .comparison-table-wrap {
    overflow-x: auto;
    border: 1px solid var(--hairline);
    border-radius: 2px;
  }

  .comparison-table {
    width: 100%;
    border-collapse: collapse;
    font-family: 'Hanken Grotesk', system-ui, sans-serif;
    font-size: 0.9rem;
  }

  .comparison-table thead th {
    padding: 1rem 1.25rem;
    font-size: 0.75rem;
    font-weight: 600;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    border-bottom: 1px solid var(--hairline);
    text-align: left;
  }

  .col-aspect { background: var(--surface-low); color: var(--midnight); width: 18%; }
  .col-agency { background: var(--surface-low); color: var(--slate-muted); width: 27%; }
  .col-cheap  { background: var(--surface-low); color: var(--slate-muted); width: 27%; }
  .col-us     { background: var(--midnight); color: var(--gold); width: 28%; }

  .comparison-table tbody tr {
    border-bottom: 1px solid var(--hairline);
    transition: background 200ms ease;
  }
  .comparison-table tbody tr:last-child { border-bottom: none; }
  .comparison-table tbody tr:hover { background: var(--surface-low); }

  .comparison-table td {
    padding: 1rem 1.25rem;
    vertical-align: top;
    line-height: 1.5;
  }

  .td-aspect {
    font-weight: 600;
    color: var(--midnight);
    font-size: 0.875rem;
  }

  .td-agency, .td-cheap {
    color: var(--slate-muted);
    font-size: 0.875rem;
  }

  .td-us {
    background: rgba(15, 23, 42, 0.02);
  }

  .td-us-inner {
    display: flex;
    align-items: flex-start;
    gap: 0.5rem;
    color: var(--midnight);
    font-weight: 600;
    font-size: 0.875rem;
  }

  .td-check {
    font-size: 1rem;
    color: var(--gold);
    flex-shrink: 0;
    margin-top: 0.1rem;
    font-variation-settings: 'FILL' 1, 'wght' 400;
  }

  /* ================== BUILDS ================== */
  .builds {
    padding: clamp(5rem, 9vw, 8rem) 0;
    background: var(--surface);
    border-top: 1px solid var(--hairline);
  }

  .build-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 1px;
    background: var(--hairline);
    border: 1px solid var(--hairline);
    border-radius: 2px;
    overflow: hidden;
  }

  .build-card {
    display: block;
    background: var(--paper);
    text-decoration: none;
    transition: background 250ms ease;
  }

  .build-card:hover { background: #fff; }

  .build-card .card-core {
    padding-bottom: 4.5rem;
    min-height: 22rem;
  }

  .build-tag {
    width: fit-content;
    margin-bottom: 1.25rem;
    padding: 0.3rem 0.85rem;
    border: 1px solid rgba(212, 175, 55, 0.3);
    border-radius: 1px;
    color: var(--gold-muted);
    font-family: 'Hanken Grotesk', system-ui, sans-serif;
    font-size: 0.7rem;
    font-weight: 700;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  .build-card h3 {
    font-size: clamp(1.5rem, 2.5vw, 2rem);
    font-weight: 600;
    margin-bottom: 0.85rem;
    line-height: 1.15;
  }

  .build-summary {
    color: var(--slate);
    font-size: 0.95rem;
    line-height: 1.65;
  }

  .build-points {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 0.55rem 1rem;
    margin: 1.5rem 0 0;
    padding: 1.5rem 0 0;
    border-top: 1px solid var(--hairline);
    list-style: none;
  }

  .build-points li {
    position: relative;
    padding-left: 1.2rem;
    color: var(--slate);
    font-family: 'Hanken Grotesk', system-ui, sans-serif;
    font-size: 0.875rem;
    line-height: 1.4;
  }

  .build-points li::before {
    position: absolute;
    left: 0;
    color: var(--gold);
    font-weight: 700;
    content: "✓";
    font-size: 0.8rem;
  }

  /* ================== PROCESS ================== */
  .process {
    padding: clamp(5rem, 9vw, 8rem) 0;
    border-top: 1px solid var(--hairline);
  }

  .steps {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 0;
    margin: 0;
    padding: 0;
    list-style: none;
    border: 1px solid var(--hairline);
    border-radius: 2px;
    overflow: hidden;
    background: var(--hairline);
    counter-reset: steps;
  }

  .steps li {
    display: grid;
    gap: 0.75rem;
    align-content: start;
    padding: clamp(2rem, 4vw, 3rem) clamp(1.5rem, 3vw, 2.5rem);
    background: var(--paper);
    position: relative;
  }

  .steps li + li {
    border-left: 1px solid var(--hairline);
  }

  .steps li::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 2px;
    background: var(--gold);
    opacity: 0;
    transition: opacity 300ms ease;
  }

  .steps li:hover::before { opacity: 1; }

  .step-num {
    color: var(--gold-muted);
    font-family: 'Hanken Grotesk', system-ui, sans-serif;
    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.12em;
  }

  .steps h3 {
    font-size: 1.3rem;
    font-weight: 600;
    line-height: 1.2;
  }

  .steps p {
    color: var(--slate);
    font-size: 0.9rem;
    line-height: 1.65;
  }

  /* ================== LOCATIONS ================== */
  .locations {
    padding: clamp(5rem, 9vw, 8rem) 0;
    background: var(--surface);
    border-top: 1px solid var(--hairline);
  }

  .city-cloud {
    display: flex;
    flex-wrap: wrap;
    gap: 0.6rem;
  }

  .city-chip {
    display: inline-flex;
    align-items: center;
    min-height: 2.6rem;
    padding: 0 1.1rem;
    border: 1px solid var(--hairline);
    border-radius: 2px;
    color: var(--midnight);
    background: var(--paper);
    font-family: 'Hanken Grotesk', system-ui, sans-serif;
    font-size: 0.875rem;
    font-weight: 500;
    text-decoration: none;
    transition: all 250ms var(--ease);
  }

  .city-chip:hover {
    border-color: var(--gold);
    color: var(--midnight);
    transform: translateY(-2px);
    box-shadow: 0 4px 12px rgba(212, 175, 55, 0.15);
  }

  .cloud-label {
    margin: 2rem 0 0.9rem;
    color: var(--slate-muted);
    font-family: 'Hanken Grotesk', system-ui, sans-serif;
    font-size: 0.75rem;
    font-weight: 600;
    letter-spacing: 0.1em;
    text-transform: uppercase;
  }

  .city-cloud-muted .city-chip {
    min-height: 2.25rem;
    padding: 0 0.9rem;
    background: transparent;
    box-shadow: none;
    font-size: 0.825rem;
  }

  /* ================== FAQ ================== */
  .faq {
    padding: clamp(5rem, 9vw, 8rem) 0;
    border-top: 1px solid var(--hairline);
  }

  .faq-list {
    border-top: 1px solid var(--hairline);
  }

  .faq-item {
    border-bottom: 1px solid var(--hairline);
  }

  .faq-item summary {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1.5rem;
    padding: 1.35rem 0;
    color: var(--midnight);
    font-family: 'Hanken Grotesk', system-ui, -apple-system, sans-serif;
    font-size: clamp(1rem, 1.4vw, 1.15rem);
    font-weight: 600;
    letter-spacing: -0.01em;
    list-style: none;
    cursor: pointer;
    transition: color 300ms ease;
  }

  .faq-item summary::-webkit-details-marker { display: none; }
  .faq-item summary:hover { color: var(--slate); }

  .faq-mark {
    display: inline-grid;
    place-items: center;
    flex-shrink: 0;
    width: 2rem;
    height: 2rem;
    border: 1px solid var(--hairline);
    border-radius: 1px;
    color: var(--gold-muted);
    font-family: 'Hanken Grotesk', system-ui, sans-serif;
    font-size: 1.1rem;
    font-weight: 300;
    transition: all 400ms var(--ease);
  }

  .faq-item[open] .faq-mark {
    transform: rotate(45deg);
    background: var(--midnight);
    border-color: var(--midnight);
    color: var(--paper);
  }

  .faq-item p {
    max-width: 54rem;
    padding: 0 0 1.5rem;
    color: var(--slate);
    font-size: 0.95rem;
    line-height: 1.8;
  }

  /* ================== CLOSING CTA ================== */
  .closing {
    padding: clamp(3.5rem, 6vw, 5.5rem) 0;
    background: var(--surface-low);
    border-top: 1px solid var(--hairline);
    text-align: center;
  }

  .closing-inner {
    max-width: 44rem;
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .closing-inner h2 {
    color: var(--midnight);
    font-size: clamp(1.85rem, 3vw, 2.5rem);
    max-width: 24ch;
    margin: 0 auto 1rem;
    line-height: 1.18;
    font-weight: 700;
  }

  .closing-note {
    max-width: 38rem;
    margin: 0 auto 2rem;
    color: var(--slate);
    font-size: 0.95rem;
    line-height: 1.7;
  }

  .closing-actions {
    display: flex;
    justify-content: center;
  }

  /* ================== RESPONSIVE ================== */
  @media (max-width: 1024px) {
    .hero-inner {
      grid-template-columns: 1fr;
      gap: 2rem;
    }
    .hero-image-panel {
      justify-self: center;
      max-width: 28rem;
    }
    .hero-badge-tr { right: 0.5rem; }
    .hero-badge-bl { left: 0.5rem; }
    .bento {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
    .work-showcase {
      grid-template-columns: 1fr;
      gap: 2.5rem;
    }
    .stat-band {
      grid-template-columns: 1fr 1fr;
    }
    .comparison-table { font-size: 0.82rem; }
  }

  @media (max-width: 768px) {
    h1 { font-size: clamp(2rem, 9vw, 2.75rem); max-width: 100%; }
    .build-grid { grid-template-columns: 1fr; }
    .steps { grid-template-columns: 1fr; }
    .steps li + li { border-left: none; border-top: 1px solid var(--hairline); }
    .service-points { grid-template-columns: 1fr; }
    .build-points { grid-template-columns: 1fr; }
    .col-agency, .col-cheap { display: none; }
    .col-aspect { width: 45%; }
    .col-us { width: 55%; }
    .closing-inner { text-align: center; }
  }

  @media (max-width: 640px) {
    .bento {
      grid-template-columns: 1fr;
    }
    .stat-band { grid-template-columns: 1fr 1fr; }
    .hero-actions { flex-direction: column; }
    .btn-primary, .btn-ghost { width: 100%; justify-content: center; }
    .work-actions { flex-wrap: wrap; }
  }

  @media (prefers-reduced-motion: reduce) {
    .marquee-track { animation: none; }
    .btn-primary, .btn-ghost, .btn-cream,
    .city-chip, .card, .build-card,
    .card-arrow, .faq-mark { transition-duration: 1ms; }
  }
</style>
