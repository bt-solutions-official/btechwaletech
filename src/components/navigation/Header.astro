---
import { Image } from "astro:assets";
import { primaryNavigation, siteBrand, siteContact } from "@data/site/site-config";
import logo from "../../assets/images/mainlogo.png";
---

<header class="site-header" id="site-header">
  <nav class="site-nav" aria-label="Primary navigation">
    <a class="brand" href="/" aria-label={`${siteBrand.name} home`}>
      <Image src={logo} alt="" height={88} densities={[1, 2]} loading="eager" />
      <span class="brand-wordmark">BtechWaleTech</span>
    </a>

    <ul class="nav-links">
      {primaryNavigation.slice(0, -1).map((item) => (
        <li><a href={item.href}>{item.label}</a></li>
      ))}
    </ul>

    <a class="nav-cta desktop-cta" href={siteContact.whatsappHref} target="_blank" rel="noopener noreferrer">
      <span>WhatsApp Us</span>
      <span class="nav-cta-arrow" aria-hidden="true">&rarr;</span>
    </a>

    <button class="nav-toggle" type="button" aria-expanded="false" aria-controls="mobile-menu" aria-label="Open menu">
      <span class="toggle-bar"></span>
      <span class="toggle-bar"></span>
    </button>
  </nav>
</header>

<div class="mobile-menu" id="mobile-menu">
  <ul>
    {primaryNavigation.map((item, i) => (
      <li style={`--i: ${i}`}><a href={item.href}>{item.label}</a></li>
    ))}
  </ul>
  <a class="nav-cta mobile-cta" href={siteContact.whatsappHref} target="_blank" rel="noopener noreferrer" style={`--i: ${primaryNavigation.length}`}>
    <span>WhatsApp Us</span>
    <span class="nav-cta-arrow" aria-hidden="true">&rarr;</span>
  </a>
</div>

<script>
  const header = document.querySelector(".site-header");
  const toggle = document.querySelector(".nav-toggle");

  if (header) {
    const onScroll = () => header.classList.toggle("scrolled", window.scrollY > 8);
    onScroll();
    window.addEventListener("scroll", onScroll, { passive: true });
  }

  if (header && toggle) {
    const setOpen = (open: boolean) => {
      header.classList.toggle("menu-open", open);
      toggle.setAttribute("aria-expanded", String(open));
      toggle.setAttribute("aria-label", open ? "Close menu" : "Open menu");
      document.documentElement.classList.toggle("nav-lock", open);
    };

    toggle.addEventListener("click", () => {
      setOpen(!header.classList.contains("menu-open"));
    });

    document.querySelectorAll(".mobile-menu a").forEach((a) => {
      a.addEventListener("click", () => setOpen(false));
    });

    window.addEventListener("keydown", (e) => {
      if (e.key === "Escape") setOpen(false);
    });

    matchMedia("(min-width: 821px)").addEventListener("change", (e) => {
      if (e.matches) setOpen(false);
    });
  }
</script>

<style>
  :global(html.nav-lock) {
    overflow: hidden;
  }

  .site-header {
    position: sticky;
    top: 0;
    z-index: 40;
    border-bottom: 1px solid transparent;
    background: transparent;
    transition:
      background-color 450ms cubic-bezier(0.32, 0.72, 0, 1),
      border-color 450ms cubic-bezier(0.32, 0.72, 0, 1),
      box-shadow 450ms cubic-bezier(0.32, 0.72, 0, 1);
  }

  .site-header.scrolled,
  .site-header.menu-open {
    border-bottom-color: rgba(15, 23, 42, 0.08);
    background: rgba(253, 252, 251, 0.88);
    box-shadow: 0 1px 0 rgba(15, 23, 42, 0.06), 0 8px 32px rgba(15, 23, 42, 0.04);
    backdrop-filter: blur(20px) saturate(160%);
    -webkit-backdrop-filter: blur(20px) saturate(160%);
  }

  .site-nav {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1.5rem;
    max-width: 1280px;
    height: 5rem;
    margin: 0 auto;
    padding: 0 clamp(1.25rem, 5vw, 5rem);
  }

  .brand,
  .nav-cta,
  .site-nav a,
  .mobile-menu a {
    text-decoration: none;
  }

  .brand {
    display: inline-flex;
    align-items: center;
    gap: 0.65rem;
    min-width: 0;
  }

  .brand img {
    display: block;
    width: auto;
    height: 2.5rem;
  }

  .brand-wordmark {
    color: #0F172A;
    font-family: 'Libre Caslon Text', Georgia, serif;
    font-size: 1.15rem;
    font-weight: 700;
    letter-spacing: -0.01em;
    line-height: 1;
    white-space: nowrap;
  }

  .nav-links {
    display: flex;
    align-items: center;
    justify-content: flex-end;
    flex: 1;
    gap: clamp(1rem, 2.5vw, 2.25rem);
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .nav-links a {
    position: relative;
    display: inline-flex;
    align-items: center;
    min-height: 2.5rem;
    color: #334155;
    font-family: 'Hanken Grotesk', system-ui, sans-serif;
    font-size: 0.8125rem;
    font-weight: 600;
    letter-spacing: 0.07em;
    text-transform: uppercase;
    white-space: nowrap;
    transition: color 300ms ease;
  }

  .nav-links a::after {
    position: absolute;
    right: 0;
    bottom: 0.5rem;
    left: 0;
    height: 1px;
    background: #D4AF37;
    transform: scaleX(0);
    transform-origin: left;
    transition: transform 350ms cubic-bezier(0.16, 1, 0.3, 1);
    content: "";
  }

  .nav-links a:hover,
  .nav-links a:focus-visible {
    color: #0f172a;
  }

  .nav-links a:hover::after,
  .nav-links a:focus-visible::after {
    transform: scaleX(1);
  }

  .nav-cta {
    display: inline-flex;
    align-items: center;
    gap: 0.55rem;
    min-height: 2.6rem;
    padding: 0 1.5rem;
    border-radius: 2px;
    color: #FDFCFB;
    background: #0f172a;
    font-family: 'Hanken Grotesk', system-ui, sans-serif;
    font-size: 0.8125rem;
    font-weight: 600;
    letter-spacing: 0.07em;
    text-transform: uppercase;
    white-space: nowrap;
    border: 1px solid #0f172a;
    transition:
      background-color 300ms ease,
      border-color 300ms ease,
      transform 300ms ease,
      box-shadow 300ms ease;
  }

  .nav-cta-arrow {
    transition: transform 300ms cubic-bezier(0.16, 1, 0.3, 1);
    display: inline-block;
  }

  .nav-cta:hover,
  .nav-cta:focus-visible {
    background: #334155;
    border-color: #334155;
    box-shadow: 0 4px 16px rgba(15, 23, 42, 0.15);
  }

  .nav-cta:hover .nav-cta-arrow,
  .nav-cta:focus-visible .nav-cta-arrow {
    transform: translateX(3px);
  }

  .nav-cta:active {
    transform: scale(0.97);
  }

  /* ---------- hamburger ---------- */
  .nav-toggle {
    display: none;
    position: relative;
    flex-shrink: 0;
    width: 2.75rem;
    height: 2.75rem;
    padding: 0;
    border: 1px solid rgba(15, 23, 42, 0.15);
    border-radius: 2px;
    background: transparent;
    cursor: pointer;
  }

  .toggle-bar {
    position: absolute;
    left: 50%;
    top: 50%;
    width: 1.15rem;
    height: 1.5px;
    border-radius: 1px;
    background: #0f172a;
    transition: transform 400ms cubic-bezier(0.32, 0.72, 0, 1);
  }

  .toggle-bar:nth-child(1) {
    transform: translate(-50%, calc(-50% - 3.5px));
  }

  .toggle-bar:nth-child(2) {
    transform: translate(-50%, calc(-50% + 3.5px));
  }

  .menu-open .toggle-bar:nth-child(1) {
    transform: translate(-50%, -50%) rotate(45deg);
  }

  .menu-open .toggle-bar:nth-child(2) {
    transform: translate(-50%, -50%) rotate(-45deg);
  }

  /* ---------- mobile menu ---------- */
  .mobile-menu {
    display: none;
    position: fixed;
    top: 4.2rem;
    right: 0;
    bottom: 0;
    left: 0;
    z-index: 39;
    flex-direction: column;
    padding: 1.5rem clamp(1.25rem, 5vw, 2rem) 2.5rem;
    background: rgba(253, 252, 251, 0.97);
    backdrop-filter: blur(28px) saturate(180%);
    -webkit-backdrop-filter: blur(28px) saturate(180%);
    opacity: 0;
    visibility: hidden;
    transition:
      opacity 400ms cubic-bezier(0.32, 0.72, 0, 1),
      visibility 400ms;
    overflow-y: auto;
    border-top: 1px solid rgba(15, 23, 42, 0.06);
  }

  .menu-open + .mobile-menu {
    opacity: 1;
    visibility: visible;
  }

  .mobile-menu ul {
    display: grid;
    margin: 0 0 1.5rem;
    padding: 0;
    list-style: none;
  }

  .mobile-menu li a {
    display: flex;
    align-items: center;
    min-height: 3.5rem;
    border-bottom: 1px solid rgba(15, 23, 42, 0.07);
    color: #0f172a;
    font-family: 'Libre Caslon Text', Georgia, serif;
    font-size: 1.4rem;
    font-weight: 400;
    letter-spacing: -0.01em;
  }

  .mobile-menu li,
  .mobile-cta {
    opacity: 0;
    transform: translateY(14px);
    transition:
      opacity 500ms cubic-bezier(0.32, 0.72, 0, 1),
      transform 500ms cubic-bezier(0.32, 0.72, 0, 1);
    transition-delay: 0ms;
  }

  .menu-open + .mobile-menu li,
  .menu-open + .mobile-menu .mobile-cta {
    opacity: 1;
    transform: translateY(0);
    transition-delay: calc(60ms * var(--i, 0) + 60ms);
  }

  .mobile-cta {
    justify-content: center;
    min-height: 3.2rem;
    font-size: 0.9rem;
    margin-top: 0.75rem;
  }

  /* ---------- breakpoints ---------- */
  @media (max-width: 820px) {
    .site-nav {
      height: 4.2rem;
      gap: 0.8rem;
    }

    .brand img {
      height: 2.2rem;
    }

    .brand-wordmark {
      font-size: 1rem;
    }

    .nav-links,
    .desktop-cta {
      display: none;
    }

    .nav-toggle {
      display: block;
    }

    .mobile-menu {
      display: flex;
    }
  }

  @media (max-width: 360px) {
    .brand-wordmark {
      display: none;
    }
  }

  @media (prefers-reduced-transparency: reduce) {
    .site-header.scrolled,
    .site-header.menu-open {
      background: #FDFCFB;
      backdrop-filter: none;
    }

    .mobile-menu {
      background: #FDFCFB;
      backdrop-filter: none;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .site-header,
    .nav-links a,
    .nav-links a::after,
    .nav-cta,
    .nav-cta-arrow,
    .toggle-bar,
    .mobile-menu,
    .mobile-menu li,
    .mobile-cta {
      transition-duration: 1ms;
    }
  }
</style>
